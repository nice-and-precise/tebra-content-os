<!-- block:implementation-steps id="implementation-step-{n}" cite="{source_id}" -->
## Step {n}: {Action in imperative}

**Time:** {realistic estimate, e.g., "10–20 minutes"}

{What the user does. One complete action. If sub-steps are needed, use a numbered list:}

1. {Sub-step 1}
2. {Sub-step 2}
3. {Sub-step 3}

**What to expect:** {The feedback signal that tells the user they did it correctly.}
<!-- /block -->

<!--
RULES:
- cite= is required. Reference the internal_doc source that verifies the described product behavior.
- Heading must be imperative voice: "Configure your billing profile" not "Billing profile configuration."
- One major action per block. Do not combine unrelated actions.
- Every step must have a time estimate.
- "What to expect" is required — it closes the loop and reduces support tickets.
- If no source exists yet, use cite="pending-source-registration" and add to draft warnings[].
-->
